# catalog.json Standard

`catalog.json` is for publishable/reusable Stackra packages.

It documents package identity, purpose, capabilities, usage guidance, maturity,
ownership, peer expectations and documentation.

It is not a deployment manifest and does not belong in services, Workers or
applications unless those artifacts are themselves publishable packages.
