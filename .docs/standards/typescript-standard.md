# TypeScript Standard

- Node.js 24 LTS for backend Node workloads.
- Strict TypeScript.
- Extend Stackra TypeScript configuration where applicable.
- Public declarations use dedicated declaration files.
- Avoid `any`; use `unknown` at unsafe boundaries.
- Explicit return types are preferred for exported functions.
- Use ESM consistently.
- Build libraries with Stackra tsup configuration.
