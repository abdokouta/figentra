# {{PACKAGE_NAME}}

> **Tier:** framework · **Kind:** feature · **Version:** 0.1.0

{{DESCRIPTION}}

## Install

```bash
pnpm add {{PACKAGE_NAME}}
```

## Usage

```ts
import {} from "{{PACKAGE_NAME}}";
```

## Development

```bash
pnpm dev          # watch mode (tsup --watch)
pnpm test         # unit tests
pnpm build        # production build
```

## Cross-references

- `catalog.json` — package metadata.
- `.kiro/steering/package-conventions.md` — canonical package shape.
