# Search — Capabilities, Permissions and Settings

Capabilities: `search.global`, `search.resource`, `search.autocomplete`, `search.suggest`, `search.index.rebuild`, `search.admin`. Settings define enabled resource types, page limits, typo/fuzzy policy, locales, ranking weights and autocomplete limits. Permissions are resolved through IAM and applied as mandatory query constraints.