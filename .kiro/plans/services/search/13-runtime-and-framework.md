# Search — Runtime and Framework

NestJS 12 on Node 24. Fastify HTTP adapter. One source tree with `api`, `consumer`, `worker`, and `scheduler` roles. `class-validator`/pipes validate transport DTOs; domain contracts remain framework-independent. Provider SDKs are isolated in infrastructure adapters.