# Search — Observability

Emit structured logs with requestId/correlationId/tenantId/resourceType/queryMode; metrics for latency, result counts, zero-result rate, autocomplete latency, indexing lag, bulk failures, rejected events, rebuild progress and provider errors; traces cover HTTP, event consumption and provider calls. Never log raw sensitive search text or document payloads by default.