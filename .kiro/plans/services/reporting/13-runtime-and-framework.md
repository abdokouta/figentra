# Reporting — Runtime and Framework

NestJS 12, Node 24, Fastify. One source tree with API, consumer, worker and scheduler roles. Transport validation is framework-level; reporting domain types and provider ports remain framework-independent. OpenSearch/AWS SDK types are confined to infrastructure adapters.