# Reporting — Configuration and Registry

Configuration: OpenSearch endpoint/index prefix, query limits, export limits, job timeouts, calculation limits, shard/refresh policy, retention, schedules and provider selection. Secrets are externalized. Registry declares reportable datasets, dimensions, metrics, index aliases and runtime roles. No undocumented index or job is permitted.