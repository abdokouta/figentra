# Reporting — Observability

Track report execution latency, OpenSearch query latency, bucket/cardinality growth, result counts, export duration/size, projection lag, event failures, queue depth, rebuild progress and cache/provider errors. Correlate all jobs with request/correlation/report execution IDs. Do not log report rows or sensitive filters by default.