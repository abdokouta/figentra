# Capability Package Plans

Reusable capabilities built on the base layer: events, identity, IAM, queue, sync, search, media, notifications, workflow, query, state, tracking, analytics, and marketing.

Identity owns authentication orchestration and identity context; IAM owns authorization; audit is a separate capability with its own service and worker.
