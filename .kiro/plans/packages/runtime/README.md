# Runtime Package Plans

Runtime adapters for Node, NestJS, browser, React, React Native, desktop, and Workers. Runtime packages adapt platform contracts to execution environments without leaking runtime globals into the core layer.
