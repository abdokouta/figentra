# Base Packages

Foundational, runtime-neutral packages: contracts, container, support, errors, config, logger, storage, cache, database, ORM, schema, pagination, state machine, pipeline, HTTP, NATS, realtime, and links.

Base packages establish infrastructure primitives and must not depend on applications or deployable services.
