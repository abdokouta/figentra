/**
 * @file example.test.ts
 * @description Example unit test for @stackra/ts-hashing.
 *
 *   Placeholder verifying the testing infrastructure boots.
 *   Replace with real unit tests as the package's API grows.
 *
 *   Unit tests should:
 *   - Test a single class or function in isolation
 *   - Mock all external dependencies
 *   - Run instantly (no async I/O, no real timers)
 *   - Follow naming: `{class-name}.test.ts`
 *
 * @example
 * ```bash
 * yarn workspace @stackra/ts-hashing test
 * ```
 */

import { describe, it, expect } from 'vitest';

describe('@stackra/ts-hashing', () => {
  it('boots the test environment', () => {
    expect(true).toBe(true);
  });
});
