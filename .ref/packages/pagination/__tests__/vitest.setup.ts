/**
 * @file vitest.setup.ts
 * @description Global test setup. Runs before every test file.
 *
 *   Imports shared DI mocking and lifecycle helpers from
 *   `@stackra/testing/setup`. Add package-specific setup below
 *   (custom matchers, fixtures, console spies, etc.).
 *
 * @see @stackra/testing/setup
 */

import '@stackra/testing/setup';
