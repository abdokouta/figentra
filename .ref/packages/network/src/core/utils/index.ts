/**
 * @file index.ts
 * @module @stackra/network/utils
 * @description Barrel exports for utility functions.
 *
 *   `defineConfig` was removed in the `@stackra/config` migration —
 *   the `registerAs('network', () => ({...}))` factory now produces
 *   the final config directly.
 */

export {};
