/**
 * @file index.ts
 * @module @stackra/settings/native/utils
 * @description Barrel for the native-only utils.
 *   `mergeNativeConfig` was removed in the ADR-0063 migration —
 *   `NativeSettingsModule.forRoot` inlines its defaults.
 */

export {};
