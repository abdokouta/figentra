/**
 * @file index.ts
 * @module @stackra/settings/react/routes
 * @description Public API barrel for the settings React route
 *   classes. Auto-discovered by `RouteLoader` at bootstrap when
 *   registered via `WebSettingsModule.forRoot()` providers.
 */

export { SettingsGroupRoute } from "./group";
export { SettingsHubRoute } from "./hub";
