/**
 * @file index.ts
 * @module @stackra/coordinator/core/utils
 * @description Barrel export for coordinator utility helpers. Config
 *   authoring lives in `@stackra/config` via `registerAs(...)` —
 *   this package no longer ships `defineConfig` / `mergeConfig`
 *   helpers.
 */

export {};
