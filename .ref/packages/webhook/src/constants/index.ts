/**
 * @file index.ts
 * @module @stackra/nestjs-webhook/constants
 * @description Barrel for constants.
 *
 *   Contains package-internal constants and DI tokens. Cross-package tokens belong in `@stackra/contracts/tokens`.
 *
 *   Add new exports as the package grows:
 *   ```typescript
 *   export { MyThing } from './my-thing.constant';
 *   ```
 */

export {};
