/**
 * @file index.ts
 * @module @stackra/nestjs-webhook/types
 * @description Barrel for types.
 *
 *   Contains package-internal type aliases. Cross-package types belong in `@stackra/contracts/types`.
 *
 *   Add new exports as the package grows:
 *   ```typescript
 *   export { MyThing } from './my-thing.type';
 *   ```
 */

export {};
