/**
 * @file index.ts
 * @module @stackra/nestjs-webhook/services
 * @description Barrel for services.
 *
 *   Contains injectable services that implement the package's public contract.
 *
 *   Add new exports as the package grows:
 *   ```typescript
 *   export { MyThing } from './my-thing.service';
 *   ```
 */

export {};
