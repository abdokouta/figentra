/**
 * @file index.ts
 * @module hooks/use-inject
 * @description useInject Hook Barrel Export
 *
 *   - {@link useInject} — Resolve a provider from the DI container (throws if not found)
 */

export { useInject } from "@/core/hooks/use-inject/use-inject.hook";
