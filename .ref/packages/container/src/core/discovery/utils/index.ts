/**
 * @file index.ts
 * @module @stackra/container/core/discovery/utils
 * @description Public API barrel for the discovery sub-domain's `utils`
 *   category.
 *
 *   Empty today — the previous `mergeDiscoveryConfig` helper was
 *   retired alongside the ADR-0063 DI-first config migration; new
 *   discovery-specific util files land here when the pattern
 *   returns.
 */

export {};
