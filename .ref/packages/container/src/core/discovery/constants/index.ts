/**
 * @file index.ts
 * @module @stackra/container/core/discovery/constants
 * @description Public API barrel for the discovery sub-domain's
 *   `constants` category.
 *
 *   Empty today — the previous `DEFAULT_DISCOVERY_CONFIG` constant
 *   was retired alongside the ADR-0063 DI-first config migration;
 *   discovery config now flows through `@stackra/config`'s
 *   `registerAs` factories. New discovery constants land here when
 *   the pattern returns.
 */

export {};
