{
  "$schema": "../../../.ref/schemas/catalog.v1.json",
  "name": "@stackra/pipeline",
  "tier": "foundation",
  "surfaces": ["core", "testing"],
  "kind": "framework-plumbing",
  "purpose": "Laravel-style middleware pipeline — fluent send/through/then API, DI integration, and Hub presets for named pipelines.",
  "capabilities": [
    "Fluent pipeline.send(x).through([...]).then(cb) API",
    "Sync + async middleware support",
    "Container-resolved middleware classes",
    "Hub presets for named, reusable pipelines",
    "Composable with @stackra/actions dispatcher",
    "Testing subpath with recording middleware"
  ],
  "when_to_use": "Composing an ordered chain of middleware around a value (HTTP request, action, event, ...).",
  "when_not_to_use": "For simple function composition use plain reduce; for event fan-out use @stackra/events.",
  "peer_deps": [
    "@stackra/container",
    "@stackra/contracts",
    "@stackra/support",
    "@stackra/testing",
    "reflect-metadata"
  ],
  "backend_pair": null,
  "size_gzip_kb": {},
  "maturity": "beta",
  "owning_agent": "framework-core-builder",
  "docs": ["packages/framework/src/pipeline/README.md"]
}
