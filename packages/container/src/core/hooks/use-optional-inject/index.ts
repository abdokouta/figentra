/**
 * @file index.ts
 * @module @stackra/container/react/hooks/use-optional-inject
 * @description useOptionalInject Hook Barrel Export
 *
 * Public API:
 *   - {@link useOptionalInject} — Resolve a provider from the DI
 *     container, or `null` when the binding is missing.
 */

export { useOptionalInject } from "./use-optional-inject.hook";
