/**
 * @file index.ts
 * @module @stackra/contracts/types/dashboard
 * @description Barrel export for dashboard types promoted to
 *   contracts (per Test A multi-consumer threshold + cycle-break for
 *   the `@Widget()` decorator).
 */

export type { WidgetCohort } from "./widget-cohort.type";
export type { WidgetPreviewType } from "./widget-preview-type.type";
export type { WidgetSpan } from "./widget-span.type";
