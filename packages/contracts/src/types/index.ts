/**
 * @file index.ts
 * @module @stackra/contracts/types
 * @description Public API barrel for cross-package type aliases.
 *   Types that can't be expressed as interfaces (unions, mapped types,
 *   tuples) live here.
 */
