import "reflect-metadata";
import { Module } from "@stackra/container";

/** Root dependency-injection composition module for the native app. */
@Module({ imports: [], providers: [], exports: [] })
export class AppModule {}
